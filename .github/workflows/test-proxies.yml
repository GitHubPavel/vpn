name: Test VPN Proxies

# Запускается вручную — кнопкой "Run workflow" (в вебе или в приложении GitHub Mobile)
on:
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install xray-core
        run: |
          curl -L -o xray.zip https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip
          mkdir -p xray-bin
          unzip -o xray.zip -d xray-bin
          chmod +x xray-bin/xray

      - name: Install Python dependencies
        run: pip install "requests[socks]"

      - name: Run proxy tests
        env:
          SUB_URL: https://raw.githubusercontent.com/zieng2/wl/main/vless_universal.txt
        run: python proxy_tester.py --sub-url "$SUB_URL" --xray "$(pwd)/xray-bin/xray" --out results.csv --workers 20

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: results
          path: results.csv
          retention-days: 7
